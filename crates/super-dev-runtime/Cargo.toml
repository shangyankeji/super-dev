[package]
name = "super-dev-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Super Dev runtime trait — the contract host drivers and HTTP runtimes implement."

[dependencies]
super-dev-spec = { workspace = true }

async-trait = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
# HTTP runtimes (OpenAI-compatible + Anthropic native). Used by the direct-API
# mode so users can point Super Dev at DeepSeek / OpenRouter / Ollama / Groq /
# the official Anthropic API without installing a host CLI.
reqwest = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
mockito = { workspace = true }
